白子哥，我确认了当前状态：两个被点名的工作树分支都尚未产生提交；汉字工作树仅有未跟踪的本地化文档。OpenAI 功能源码实际位于另一个名称多一层 `worktree-`、正被活跃 Claude 会话锁定的工作树中，包含未提交的 `src/components/StudySetup.tsx` 改动；按你的选择，暂时保留且不触碰它。

执行方案：
1. 在当前主工作树中，先保护现有未跟踪的 `.claude/`、`.zcode/` 内容，不删除、不覆盖。
2. 基于当前 `main` 创建并切换到集成分支 `feat/hanzi-openai-custom-mode`。
3. 从 `hanzi-chinese-translation` 工作树取得 `docs/zh-CN-localization.md`，在集成分支提交为汉字/中文翻译部分的当前可用成果。
4. 从已点名的 `openai-custom-mode-feature` 工作树取得 `docs/openai-custom-model.md`，同时记录该分支没有功能源码提交；不会做无效 merge。
5. 不修改、不解锁、不删除仍被活跃会话锁定的额外 OpenAI 工作树。待其会话结束、源码改动可安全保留后，再将其功能改动整合到此分支。
6. 对已整合的两个未锁定工作树执行 Git worktree 清理；不会删除任何工作目录中的文件，清理前会确认工作树都没有剩余未纳入的内容。
7. 查看变更和 Git 状态，报告新分支、提交、已清理/保留的工作树，以及锁定工作树后续需完成的操作。

说明：由于真正的 OpenAI 源码仍在锁定且未提交的工作树内，本次无法完整合并该功能；会先完成文档与分支整合，严格保留该工作树，避免丢失活跃会话的改动。