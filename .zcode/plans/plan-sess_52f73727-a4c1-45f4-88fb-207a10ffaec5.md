白子哥，两个需求目前都只有方案文档，没有可复用的功能实现。已确认额外锁定 Worker Tree 中的 OpenAI 改动本身结构不完整（把 `<input>` 嵌进 `<select>`，且误影响全部 Provider），不会采纳。

## 目标与边界

- 将默认产品体验完整改为**简体中文**：研究者端、参与者端、模型提示词/兜底内容、内置样例和演示数据、面向用户的 API 返回提示、页面元信息与日期格式。
- 不引入 i18n、语言切换或 locale 路由；不翻译用户输入、已保存研究/访谈/分析内容、模型 ID、环境变量、URL、TypeScript/API/JSON 字段、状态枚举和结构化输出键。
- OpenAI 改为研究配置页的任意模型 ID 文本输入；Gemini/Claude 维持下拉选择。
- 按你的选择，修复 Hosted/BYOK 场景中遗漏传递 `openaiApiKey` 的调用链问题；OpenAI 的综合分析模型策略仍保持现有固定 `gpt-4o`，不把自定义聊天模型扩大到综合分析。
- CSV 列标题保留英文，保证数据处理兼容性；页面和 Markdown 导出的固定文字改为中文。

## 实施步骤

1. **统一中文显示与元信息**
   - 更新 `src/app/layout.tsx` 的 title、description 和 `lang="zh-CN"`。
   - 将研究者页面内显式 `en-US` 日期格式改为 `zh-CN`；用户可见 Markdown 导出的日期/固定标题改为中文。

2. **汉化研究者端的内置界面与提示**
   - 修改 `Login`、`OAuthLogin`、`Onboarding`、`Settings`、`StudyList`、`StudySetup`、`StudyDetail`、`Dashboard`、`InterviewDetail`、`Synthesis`、`Export`。
   - 覆盖按钮、标签、占位符、空/加载/成功/失败状态、浏览器确认框、模型说明、内置默认知情同意和研究配置表单文案。
   - 保留服务商、模型 ID、API Key、`OPENAI_BASE_URL` 等技术标识原样；所有保存的数据插值保持原值。

3. **实现 OpenAI 任意模型 ID 与 Hosted/BYOK 完整调用**
   - 在 `StudySetup` 中为 OpenAI 单独渲染样式一致的文本输入框，默认 `gpt-4o-mini`；Gemini/Claude 继续使用现有下拉框。
   - OpenAI 模型在构建配置前 `trim()`；空白模型会禁用开始、预览、保存与生成链接入口，输入本身设为必填。
   - 保持编辑预填、后续研究继承、参与者链接快照和 Provider 透传的既有数据流；不添加自定义模式状态或哨兵值。
   - 在访谈、开场白、单次/汇总综合分析及后续研究的全部 `getInterviewProvider()` 调用中传递 `context.openaiApiKey`，修复 Hosted/BYOK 的 OpenAI Key 缺失。
   - 同步配置状态类型与提示，使 OpenAI Key 缺失能够以与 Gemini/Claude 一致的方式被界面识别。

4. **汉化参与者端与内置样例内容**
   - 修改 `Consent`、`InterviewChat`、`PreviewBanner` 与 `src/app/p/[token]/page.tsx` 的壳层、进度、错误和完成态；研究者自定义知情同意文本原样显示。
   - 汉化 `store.ts` 的“加载示例研究”、`types.ts` 中模型描述，以及 `demoData.ts` 的预置研究、画像、访谈记录、行为数据和分析内容。
   - 保持所有 demo ID、时间戳、状态值、对象形状与引用不变；同步汉化 `topicAreas` 和全部 `BehaviorData` 键，避免仪表盘统计断链。

5. **汉化模型输出与服务端用户提示**
   - 更新 `src/lib/prompts/interview.ts`、`greeting.ts`、`synthesis.ts`：明确要求模型用简体中文访谈/分析，原样插入研究者和参与者数据。
   - 仅翻译自然语言指令、示例值和回退文案；严格保留 JSON key、tool 名、schema、required 数组、枚举/状态值。
   - 将 `ai.ts`、`geminiService.ts`、`InterviewChat` 与三家 provider 的后续研究生成提示/回退内容同步改为中文。
   - 审核 `src/app/api` 的产品自有错误/成功/警告文本和服务端 Markdown 导出标题；保留 API JSON 形状、字段名、状态码和第三方原始错误。CSV header 不改。

6. **验证与残留审计**
   - 对 `src/app`、`src/components`、`src/lib`、`src/services` 做残留英文审计，只保留有意的技术常量、代码标识符、模型/服务商、URL/环境变量、枚举与第三方错误。
   - 运行 `npm run lint` 和 `npm run build`。
   - 在本地浏览器检查：登录/研究配置中文文案、OpenAI 文本输入默认值和任意模型输入、空白拦截、Provider 来回切换、Gemini/Claude 下拉回归、预览流的参与者中文界面。
   - 由于当前环境没有 `ADMIN_PASSWORD`、KV 和真实 provider 凭据，保存研究、生成真实链接、加载 demo、Hosted/BYOK 和真实 AI 调用将做静态/构建验证并明确标注为无法在本地端到端完成；不会伪称已验证。

预计主要触及 UI、提示词、演示数据、provider 路由与 API 提示，属于多文件但不改变数据模型或产品流程的定向实现。