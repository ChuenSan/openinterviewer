白子哥，两个目标 Worktree 已经存在，且分别对应所需任务，因此不另建目录，避免重复或干扰现有锁定会话：

- 汉化：`/Users/mac/projects/0GitHub-repo/openinterviewer/.claude/worktrees/hanzi-chinese-translation`（分支 `worktree-hanzi-chinese-translation`）
- OpenAI 自定义模型：`/Users/mac/projects/0GitHub-repo/openinterviewer/.claude/worktrees/openai-custom-mode-feature`（分支 `worktree-openai-custom-mode-feature`）

## 1. 全产品中文

在汉化 Worktree 中，按现有“固定 UI 文案直接写在组件中”的结构做针对性汉化，不引入 i18n 库、路由或语言切换机制（项目目前没有这类架构，且目标是中文产品）。

1. 更新根页面元数据与 `html lang` 为简体中文：`src/app/layout.tsx`。
2. 汉化研究者端固定界面文案：研究列表、研究配置、研究详情、仪表盘、访谈详情、综合分析、导出、登录、引导与设置等组件。
3. 汉化参与者端固定界面：同意页、访谈聊天、预览横幅及 token 参与者页面中的加载、异常、完成提示。
4. 将 AI 面试开场、访谈行为、单份与汇总分析等内置提示词改为中文，确保默认生成的面试与分析过程使用中文；保留结构化字段名、模型名和机器读取的 JSON 契约不变。
5. 将演示模式预置的研究配置、访谈记录、同意说明和分析示例改为中文，以消除用户首次进入时残留的英文内容。
6. 不翻译用户输入/持久化的研究内容、问题、个人资料字段值或历史访谈内容，避免意外改写用户数据。
7. 使用项目现有 `lint` 与 `build` 脚本进行验证；项目没有既有测试框架，不额外引入测试依赖。

主要覆盖文件包括：`src/components/StudySetup.tsx`、`StudyDetail.tsx`、`StudyList.tsx`、`InterviewChat.tsx`、`Consent.tsx`、`Synthesis.tsx`、`Export.tsx`、`Dashboard.tsx`、`InterviewDetail.tsx`、`Login.tsx`、`Onboarding.tsx`、`Settings.tsx`、`PreviewBanner.tsx`、`src/app/p/[token]/page.tsx`、`src/lib/prompts/*.ts`、`src/lib/demoData.ts` 与 `src/app/layout.tsx`。

## 2. OpenAI 模型改为纯自定义输入

在 OpenAI Worktree 中，只改研究配置界面：

1. 修改 `src/components/StudySetup.tsx` 的模型控件分支：选择 OpenAI 时显示必填的模型 ID 文本输入框，直接绑定现有 `aiModel` 状态；Gemini 和 Anthropic 继续使用原本的预设下拉框。
2. 保留现有切换供应商时重设各供应商默认模型的行为；切到 OpenAI 后，输入框预填 OpenAI 默认模型，用户可直接覆盖。
3. 确保编辑已有研究或加载任意 OpenAI 自定义模型名时，输入框完整显示并可继续编辑，不受原预设列表限制。
4. 不修改 `StudyConfig`、持久化 API、签名链接或 OpenAI Provider：这些路径已经以任意字符串保存、传递和调用 `aiModel`，不存在后端模型白名单。
5. 不处理与此需求无关的 Hosted/BYOK OpenAI key 传递遗漏问题；同时保留 Worktree 内现有未跟踪的 `docs/openai-custom-model.md`，不覆盖、不删除。
6. 运行 `lint` 与 `build` 验证。项目未配置组件测试框架，因此不新增依赖或测试基础设施。

主要改动文件：`src/components/StudySetup.tsx`。